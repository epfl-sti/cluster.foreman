# = Class: epflsti::params
#
# The "params" Puppet pattern.
#
# This class computes some default values from the platform-specific
# details etc. The main class (in init.pp) inherits from it so as to
# spare itself the clutter.
#
# There isn't much going on in here just yet.
class epflsti::params {
}
